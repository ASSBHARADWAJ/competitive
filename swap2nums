class swap
{
public static void main(String args[])
{
int a=10;
int b=20;
System.out.println("Before Swapping Elements are:-"+a" "+b);

//method-1--assigning the value of a to c(1st) next b value to a and then c value to b by using a third variable
int c=a;
a=b;
b=c;

//method-2--by using addition and subraction operators
a=a+b;
b=a-b;
a=a/b;

//method-3--by using multiplication and division operators
a=a*b;
b=a/b;
a=a/b;

//method-4--it is used a ternary operator so it gives fast results because it works with bytes
a=a^b;
b=a^b;
a=a^b;

//method-5--it is a single line statement
b=a+b-(a=b);

System.out.println("After Swapping elements are:-"+a" "+b);
}
}
